# Manifests

SHA256 hash manifests of reference O2R contents, one per ROM version.

Generated by `manifest.sh` from reference O2R files. Used by test scripts to verify Torch output matches the reference.

These files are gitignored and must be generated locally.
